Етап 1 — Перевірка ролей і доступів

Тут перевіряємо не тільки ролі, а й усю матрицю доступів:

PERMISSIONS
Access.hasPermission()
Access.canAccessKnowledgeCategory()
roles
denyPermissions
allowPermissions
перевірка всіх модулів:
БК
Знання
3D Models
Reviews
перевірка всіх окремих дозволів (фото, відео, документи тощо).

Результат — таблиця з тим, що є, що не використовується і що потрібно додати.

Етап 2 — Аудит безпеки

Я б перевірив:

авторизацію;
зберігання токена;
перевірку JWT;
Cloudflare Worker;
KV;
RBAC;
доступ до медіафайлів напряму;
можливість обійти фронтенд;
XSS;
відкриті API;
конфігурацію;
структуру директорій;
усе, що може стати проблемою перед релізом.

Після цього сформую список:

🔴 критично;
🟠 бажано;
🟢 можна залишити до наступного релізу.
Етап 3 — Модуль 3D моделей

Коли будемо впевнені, що архітектура доступів правильна.

Тоді вже:

створимо права;
додамо їх у RBAC;
зробимо модуль одразу на правильній архітектурі.


====================================

План роботи

Я пропоную розбити аудит на кілька етапів, щоб нічого не пропустити.

Етап 1 — Архітектура

Перевірю:

структуру директорій;
розділення відповідальності між модулями;
залежності між модулями;
дублювання коду;
конфігурацію;
чи відповідає структура тій архітектурі, яку ми вже вибудували.

Результат:

✅ Архітектура

або перелік того, що потрібно покращити.

Етап 2 — RBAC

Перевірю повністю:

functions/_lib/access.js
config/access-client.js
roles
PERMISSIONS
allowPermissions
denyPermissions
окремі права Knowledge
окремі права BK
Models3D
Reviews
чи є "мертві" права
чи є права, які не використовуються
чи є перевірки, яких не вистачає.

Результат:

✅ RBAC

з повною картою доступів.

Етап 3 — Безпека

Це буде найдетальніший аудит.

Перевірю:

JWT;
Worker;
API;
KV;
frontend;
localStorage / sessionStorage;
прямий доступ до медіа;
XSS;
HTML injection;
перевірки на сервері;
чи можна обійти RBAC;
чи можна викликати API напряму.

Після цього буде список:

🔴 критично

🟠 бажано

🟢 можна залишити
Етап 4 — Frontend

Перевірю:

структуру HTML;
повторюваний код;
JS;
CSS;
продуктивність;
читабельність;
що можна спростити.